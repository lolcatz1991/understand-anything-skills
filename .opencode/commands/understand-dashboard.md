---
description: Launch the interactive web dashboard to visualize the knowledge graph for this project
---

Load the `understand-dashboard` skill and start the interactive visualization dashboard. $ARGUMENTS

Check that `.understand-anything/knowledge-graph.json` exists, locate the dashboard package, start the Vite dev server with `GRAPH_DIR` pointing at this project, and open the browser at http://localhost:5173.
